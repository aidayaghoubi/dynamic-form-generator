import styled from "@emotion/styled"

const EmptyWrapper = styled.div `
position: absolute;

`

const EmptyForm = () =>{



    return <EmptyWrapper>
        you can add input from the left side
    </EmptyWrapper>
}

export default EmptyForm